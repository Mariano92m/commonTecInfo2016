import os

class Complejo:
	def __init__(self, nombre, direccion, largo, ancho):
		self.nombre nombre
		self.direccion direccion
		self.largo dimensiones

	def mostrarNom():
		print("El nombre del Complejo es %s" %(self.nombre))
	def mostrarDir():
		print("La direccion es %s" %(self.direccion))
	def mostrarDim():
		print("El las dimensiones son %s" %(self.pene))